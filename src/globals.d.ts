// Custom global type declarations for MythSeeker

declare global {
  // Global analytics (if not already provided by a types package)
  var gtag: (...args: any[]) => void;
  // Add any other custom globals here
}

export {}; 